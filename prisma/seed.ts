import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();

async function seedflection() {
    const election = await prisma
}

async function main() {
    console.log('seeding database')    
}